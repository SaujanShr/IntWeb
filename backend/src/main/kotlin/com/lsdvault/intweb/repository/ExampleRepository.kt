// Connector between the Java example object and the database.
// We won't we using these right now, but good to know they exist.
// We don't even have a database tho so I'm not gonna bother implementing this.

// @Repository
// Repository class etc